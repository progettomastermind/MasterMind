class GameConfig{
    public int length;
    public int attempts;
    public int colors;

    GameConfig(int length, int attempts, int colors){
        this.length = length;
        this.attempts = attempts;
        this.colors = colors;
    }
}